<%= Welcome, @user.username %>

<% @user.movies.each do |movie| %>
    <h1><%= movie.title %></h1>

    <% movie.genre.each do |genre| %>
        <li><%= genre.name %></li>
    <% end %>

    <% movie.actors.each do |actor| %>
        <li><%= actor.name %></li>
    <% end %>
<% end %>